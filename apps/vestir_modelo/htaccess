SetEnv NOMBRE_VARIABLE valor
AddType text/javascript .mjs

<FilesMatch "\.json$">
  Require all denied
</FilesMatch>

# La clave FLUX (F) se lee del .htaccess RAÍZ de Hostinger.
# La clave de Gemini (GEMINI_API_KEY) ya NO se usa aquí.
# Esta app usa 100% FLUX vía proxy.php con getenv('F').
